# Review contract specification

This module is the starting point for discovering the simplest compositional
meaning of a Lineage review contract. It intentionally introduces no semantic
types yet: those types should be chosen through denotational design rather than
copied from the preliminary data-structure inventory.

```agda
{-# OPTIONS --safe #-}

module Lineage.Specification.ReviewContract where
```
