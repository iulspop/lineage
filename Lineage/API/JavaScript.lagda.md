# JavaScript API boundary

This module will adapt the pure Lineage API for JavaScript hosts. Browser,
storage, networking, clock, and randomness effects remain outside the core.

```agda
module Lineage.API.JavaScript where
```
