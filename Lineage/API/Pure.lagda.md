# Pure Lineage API

This module will expose the small, total API shared by compiled hosts without
introducing host-specific effects.

```agda
{-# OPTIONS --safe #-}

module Lineage.API.Pure where
```
